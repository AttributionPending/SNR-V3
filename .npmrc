# eslint-plugin-react-hooks@7 declares an older eslint peer range than the eslint 10
# we pin; the combination works in practice. Keep installs reproducible (and CI green)
# without requiring every `npm install` to pass --legacy-peer-deps by hand.
legacy-peer-deps=true
